# The script transfer the vector_np_lam12 into vectors with different lambda value

import numpy as np

def vector_transfer(reset_reset_psepssm_lam = 1, reset_pseaac_lam = 1)

vector_np_lam12 = np.load('')